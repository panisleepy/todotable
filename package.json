{
  "name": "vibe-diary",
  "version": "0.2.0",
  "private": true,
  "description": "Working Table Diary — transparent desktop side dock",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "dist": "node scripts/build-dist.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
